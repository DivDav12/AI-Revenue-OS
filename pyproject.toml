[project]
name = "revenue-os"
version = "0.1.0"
description = "Legal, automated multi-agent AI revenue ecosystem"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
llm = ["anthropic>=0.40"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
